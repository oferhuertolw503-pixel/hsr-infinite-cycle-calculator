# 案例研究：姬子/刻律/罗刹/缇宝 循环建模（样例截图）

> 本文把 `样例/` 下的四张截图（已归档为 `docs/screenshots/case_*.png`）
> 作为一个连贯案例处理：同一位作者的建模过程，从四节点初版到七节点
> 完整模型再到击杀回能修正。全部数值结论由本仓库工具链在**转录实测
> 矩阵**上复算得出；每张截图的核验状态如实标注。

## 1. 案例背景

- **队伍语境**：姬子（H）、刻律（C）、罗刹、缇宝（T）；
  事件节点 = 姬子行动 `H`、姬子大招 `H_U`、刻律行动 `C`、军功 `M`、
  刻律大招 `C_U`、缇宝行动 `T`、缇宝大招 `T_U`。
- **机制说明**（截图文字）：姬子每 hit 触发追击/强化 E/缇宝天赋；
  刻律行动给 2 军功（上限 4）；刻律大招 3 hits 转军功；罗刹场每回合
  给 1 SP；**N = 敌人数量**。
- **建模三阶段**：

| 阶段 | 截图 | 模型 | 谱半径 | 核验 |
|---|---|---|---|---|
| 一 | `case_4node_decay.png` | 四节点初版（H/H_U/C/M） | 0.88856（N=5,截图矩阵） | ✅ **已恢复**（见 §4） |
| 二 | `case_4node_kill.png` | 四节点击杀回能（H/H_U/T/T_U） | 0.9822/0.99683/1.01088/**1.02442**（N=2..5） | ✅ 已转录入库 |
| 三 | `case_7node_matrix.png`、`case_matrix_definition.png` | 七节点完整模型 | **1.00522**..1.04432（N=2..5） | ✅ 已转录入库 |

已入库的实测矩阵：`examples/theory_document/four_node_model_N5.json`
（阶段一截图矩阵）、`seven_node_real_family.json`、
`four_node_kill_real_family.json`（均由 `tools/generate_theory_examples.py`
按精确分式确定性再生成）。

## 2. 案例演进与理论对照

**阶段一（衰减）**：四节点初版 ρ<1（N=5 时截图矩阵计算得 0.88856；
文档 §6 标注 0.88353 与之不符，**以截图矩阵为准**，见 §4）。
定理 1 直接适用：纯线性模型必然衰减，初始资源只能延后断轴。

**阶段二（"可无限循环"？）**：聚焦姬子+缇宝并加入击杀回能后，
截图在 N=5 得 ρ=1.02442，并给出结论"**可无限循环**"。这正是理论
文档 §6 修正的表述：ρ>1 只说明线性、无上限、条件恒成立的近似下
存在增长方向。本项目复算该实测矩阵还得到截图未记录的信息——

- **临界目标数是 4 不是 5**（§8"目标数从哪里进入系统"）：
  N=3 时 ρ=0.99683<1 仍衰减，N=4 时 1.01088 首次越过 1；
  `critical_parameter` 返回 `critical_key=4, sub_critical_key=3`。
- **主导边是 H_U→H_U**（姬子大招自循环）：弹性 d ρ/d a = 0.6987
  全矩阵最大；整边移除后 ρ 跌到 0.674，深衰减。最小份额节点是
  T（0.116）——缇宝行动是先行断粮候选（§4）。
- 增长翻倍时间 ln2/ln(1.02442) ≈ **28.7 轮**：即使线性增长成立，
  增长也很慢，实战必须靠上限内的周转而非指数积累。

**阶段三（略超临界）**：七节点完整模型 N=2..5 全部 ρ>1
（1.00522..1.04432），N 只进入 T_U 行（`(4.5N+5)/97.5`、`6N/97.5`、
`1.5N/97.5`）——§5.2"矩阵条目本身随 N 改变"的直接实证。
但 N=2 仅 1.00522（翻倍约 133 轮），且瓶颈分析显示脆弱边同样是
H_U→H_U（移除后 ρ=0.765）：案例的"永动"完全悬在少数几条资源边上。

## 3. 版本矩阵库中的案例

`python main.py examples/theory_document/version_matrix_library.json --library`
把本案例的各阶段（含重构与实测、击杀持续/不持续、N=2..5）并排对比：
regime 不一致 → 结论上下文绑定、禁止外推（§7 步骤 8）；
3 种事件粒度 → ρ 不可跨粒度排名（§7 步骤 1）。

## 4. 阶段一矩阵的恢复（以截图为准）

`case_4node_decay.png` 的四节点矩阵**已按截图恢复**（节点 H/H_U/C/M）：

- **反解依据**：截图矩阵的 Perron 向量 w=(0.207, 0.542, 0.091, 0.161)
  （3 位小数）与 ρ=0.88856，逐行解 `A w = ρ w`（候选值取作者分数体系）；
- **恢复矩阵 A\***（已入库 `four_node_model_N5.json`）：
  ρ(A\*)=0.88855，H_U 行 [3/5,3/5,0,1/5] 与七节点族同源，
  M 行 [1/12,1/20,1/5,1/2] 反解唯一；
- **歧义单元格锁定为 H_U→M**：读 1/5 得 0.88856，读 1/4 得 0.89640
  （截图记录 0.89622，差 0.0002 为 w 截断所致）——case_study 记录的
  两个读数被同时复现；
- **与文档 §6 标注值的矛盾**：标注 0.88353 与显示矩阵不符（ρ 差 0.005、
  标注向量 (0.32,0.76,0.30,0.46) 与 w 也完全不同），二者来自两次不同
  计算输入；**以截图矩阵为准**（用户确认）。标注值保留于 JSON 的
  `documented_rho_doc` 字段供对照；
- **N=2..4 特征值表**（0.82543..0.88353 近似线性）：截图只显示了 N=5
  的矩阵，该表无矩阵依据。按"全局缩放"构造 B(N)=ρ(N)/0.88353·B 可复现
  该表（差 <5×10⁻⁷，见对话框推导），但属构造解释而非截图证据，仍列为
  待核对项；
- 恢复的 A\* 条目由 3 位向量反解得到，**分数形式待原图核对**。

## 5. 复现命令

```bash
python main.py examples/theory_document/four_node_kill_real_family.json --family
python main.py examples/theory_document/seven_node_real_family.json --family
python main.py examples/theory_document/four_node_model_N5.json --sensitivity --repair
python main.py examples/theory_document/version_matrix_library.json --library
```

## 6. 案例结论

1. 本案例的"永动"在矩阵层需要：击杀回能族 **N≥4**、或七节点族
   **N≥2** 且 T_U 行的 N 依赖转移全部兑现；
2. 全案例最关键的资源边是 **H_U→H_U**（弹性最大、最脆弱），
   最先断粮的节点是 **T**（最小 Perron 份额）；
3. ρ>1 到实战永动之间仍隔着 §5 的上限、阈值、时序与敌方行动四类
   约束——这正是本项目模拟层（`--team` / `--search`）存在的意义。
